<UserControl x:Class="VulpesX.Modules.Default.Tables.Production.CalendarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:VulpesX.Modules.Default.Tables.Production"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate x:Key="ListTemplate">
            <StackPanel Orientation="Vertical">
                <TextBlock FontSize="16" FontWeight="Bold" Text="{Binding Descrizione}" />
                <TextBlock FontSize="14" Text="{Binding ID}" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="AppointmentTemplate">
            <Grid Margin="-3,0,-2,0" Background="{Binding Appointment.Background}">
                <TextBlock FontFamily="Segoe UI Semibold"
                           FontSize="12"
                           Foreground="White"
                           Text="{Binding Appointment.Subject}"
                           ToolTip="{Binding Appointment.Body}" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <telerik:RadBusyIndicator x:Name="bsyGrid"
                              Grid.Row="4"
                              BusyContent="Caricamento dati in corso..."
                              FontSize="40"
                              Foreground="{DynamicResource VulpesXBrush}"
                              IsBusy="False"
                              OverlayStyle="{StaticResource WaitOverlayStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50*" />
                    <ColumnDefinition Width="50*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{StaticResource VulpesXTitle}" Text="CALENDARIO" />

                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Name="cmdGenera"
                            Margin="5"
                            Click="cmdGenera_Click"
                            Content="GENERA CALENDARIO"
                            Style="{StaticResource VulpesXInsertButton}" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="300*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Background="{DynamicResource VulpesXBrush}">
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="20"
                                   Foreground="White"
                                   Text="RISORSE">
                            <TextBlock.LayoutTransform>
                                <RotateTransform Angle="-90" />
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </Grid>
                    <telerik:RadListBox Name="listMachine"
                                        Grid.Column="1"
                                        ItemTemplate="{StaticResource ListTemplate}"
                                        ItemsSource="{Binding Risorse}"
                                        SelectedItem="{Binding SelectedRisorsa, Mode=TwoWay}" />
                </Grid>
                <Grid Grid.Column="2">
                    <telerik:RadScheduleView Name="schedule"
                                             Grid.Row="1"
                                             AppointmentCreating="schedule_AppointmentCreating"
                                             AppointmentDeleting="schedule_AppointmentDeleting"
                                             AppointmentEditing="schedule_AppointmentEditing"
                                             AppointmentItemContentTemplate="{StaticResource AppointmentTemplate}"
                                             ShowDialog="schedule_ShowDialog"
                                             VisibleRangeChanged="schedule_VisibleRangeChanged">
                        <telerik:RadScheduleView.ViewDefinitions>
                            <telerik:MonthViewDefinition AllowCustomAppointmentSize="True" />
                        </telerik:RadScheduleView.ViewDefinitions>
                    </telerik:RadScheduleView>
                </Grid>
            </Grid>
        </Grid>
    </telerik:RadBusyIndicator>
</UserControl>
