<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dock="clr-namespace:Telerik.Windows.Controls.Docking;assembly=Telerik.Windows.Controls.Docking"
                    xmlns:grid="clr-namespace:Telerik.Windows.Controls.GridView;assembly=Telerik.Windows.Controls.GridView"
                    xmlns:mat="clr-namespace:Telerik.Windows.Controls.MaterialControls;assembly=Telerik.Windows.Controls"
                    xmlns:pivot="clr-namespace:Telerik.Windows.Controls.Pivot;assembly=Telerik.Windows.Controls.Pivot"
                    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
                    xmlns:telerikAnimation="clr-namespace:Telerik.Windows.Controls.Animation;assembly=Telerik.Windows.Controls"
                    xmlns:telerikDocking="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls.Docking">

    <ControlTemplate x:Key="DistinctFilterControlTemplate" TargetType="grid:FilteringDropDown">
        <Grid>
            <Button x:Name="PART_DropDownButton" AutomationProperties.Name="FilterDropDownButton">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter />
                    </ControlTemplate>
                </Button.Template>
                <Border Padding="3,0,0,0" Background="Transparent" Cursor="Hand">
                    <Grid Cursor="Hand">
                        <Path Width="12"
                              Height="11"
                              Margin="2"
                              Data="M0,0 L11,0 L7,5 L7,9 L4,9 L4,5 z"
                              Fill="{TemplateBinding BorderBrush}"
                              Stretch="Fill" />
                        <Path Width="12"
                              Height="11"
                              Margin="2"
                              Data="M0,0 L11,0 L7,5 L7,9 L4,9 L4,5 z"
                              Fill="OrangeRed"
                              Stretch="Fill"
                              Visibility="{TemplateBinding FunnelFillVisibility}" />
                    </Grid>
                </Border>
            </Button>
            <Popup x:Name="PART_DropDownPopup"
                   AllowsTransparency="True"
                   PopupAnimation="Slide"
                   StaysOpen="True" />
        </Grid>
    </ControlTemplate>
    <Style x:Key="FilteringDropDownStyle" TargetType="grid:FilteringDropDown">
        <Setter Property="Template" Value="{StaticResource DistinctFilterControlTemplate}" />
        <Setter Property="BorderBrush" Value="{telerik:VisualStudio2013Resource ResourceKey=StrongBrush}" />
    </Style>
    <Style BasedOn="{StaticResource FilteringDropDownStyle}" TargetType="grid:FilteringDropDown" />

    <Style x:Key="RadWatermarkTextBoxMenuSearchStyle" BasedOn="{StaticResource RadWatermarkTextBoxStyle}" TargetType="telerik:RadWatermarkTextBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik:RadWatermarkTextBox">
                    <Grid x:Name="RootElement">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition x:Name="PART_LabelRowDefinition" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border x:Name="BorderVisual"
                                Grid.RowSpan="2"
                                Grid.ColumnSpan="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mat:MaterialAssist.CornerRadius}" />
                        <Border x:Name="ReadOnlyVisual"
                                Grid.RowSpan="2"
                                Grid.ColumnSpan="2"
                                Background="{TemplateBinding ReadOnlyBackground}"
                                BorderBrush="{telerik:FluentResource ResourceKey=ReadOnlyBorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mat:MaterialAssist.CornerRadius}"
                                IsHitTestVisible="False"
                                Visibility="Collapsed" />
                        <Border x:Name="FocusVisual"
                                Grid.RowSpan="2"
                                Grid.ColumnSpan="2"
                                Background="{DynamicResource VulpesXFirstAlternativeBrush}"
                                BorderBrush="{DynamicResource VulpesXFirstAlternativeBrush}"
                                BorderThickness="{telerik:FluentResource ResourceKey=FocusThickness}"
                                CornerRadius="{TemplateBinding mat:MaterialAssist.CornerRadius}"
                                IsHitTestVisible="False"
                                Visibility="Collapsed" />
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Grid.RowSpan="2"
                                      Margin="1"
                                      Padding="{TemplateBinding Padding}"
                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                      mat:MaterialAssist.CornerRadius="{TemplateBinding mat:MaterialAssist.CornerRadius}"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      IsTabStop="False"
                                      Opacity="{telerik:FluentResource ResourceKey=InputOpacity}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <ContentControl x:Name="WatermarkVisualElement"
                                        Grid.RowSpan="2"
                                        Padding="{TemplateBinding Padding}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding WatermarkContent}"
                                        ContentTemplate="{TemplateBinding WatermarkTemplate}"
                                        FontStyle="Italic"
                                        Foreground="{TemplateBinding Foreground}"
                                        IsHitTestVisible="False"
                                        IsTabStop="False"
                                        Opacity="{telerik:FluentResource ResourceKey=InputOpacity}"
                                        Visibility="Hidden">
                            <ContentControl.Template>
                                <ControlTemplate TargetType="ContentControl">
                                    <ContentPresenter Margin="{TemplateBinding Padding}" />
                                </ControlTemplate>
                            </ContentControl.Template>
                        </ContentControl>
                        <ContentPresenter x:Name="AdditionalContentHost"
                                          Grid.RowSpan="2"
                                          Grid.Column="1"
                                          Content="{TemplateBinding AdditionalContent}"
                                          ContentTemplate="{TemplateBinding AdditionalContentTemplate}" />
                        <telerik:Label x:Name="PART_LabelVisualElement"
                                       Grid.RowSpan="2"
                                       Content="{TemplateBinding Label}"
                                       Style="{TemplateBinding LabelStyle}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource VulpesXBrush}" />
                            <Setter TargetName="PART_ContentHost" Property="Opacity" Value="1" />
                            <Setter TargetName="WatermarkVisualElement" Property="Opacity" Value="1" />
                            <Setter TargetName="BorderVisual" Property="Background" Value="{DynamicResource VulpesXFirstAlternativeBrush}" />
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter Property="BorderThickness" Value="{telerik:FluentResource ResourceKey=FocusThickness}" />
                            <Setter TargetName="PART_ContentHost" Property="Opacity" Value="{telerik:FluentResource ResourceKey=ReadOnlyOpacity}" />
                            <Setter TargetName="WatermarkVisualElement" Property="Opacity" Value="{telerik:FluentResource ResourceKey=ReadOnlyOpacity}" />
                            <Setter TargetName="ReadOnlyVisual" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DisabledBackground}" />
                            <Setter TargetName="PART_ContentHost" Property="Opacity" Value="{telerik:FluentResource ResourceKey=DisabledOpacity}" />
                            <Setter TargetName="WatermarkVisualElement" Property="Opacity" Value="{telerik:FluentResource ResourceKey=DisabledOpacity}" />
                        </Trigger>
                        <Trigger Property="IsWatermarkVisible" Value="True">
                            <Setter TargetName="WatermarkVisualElement" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Foreground" Value="{telerik:FluentResource ResourceKey=MarkerMouseOverBrush}" />
                            <Setter TargetName="BorderVisual" Property="Background" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mat:MaterialAssist.MouseOverBrush), Mode=OneWay}" />
                            <Setter TargetName="PART_ContentHost" Property="Opacity" Value="1" />
                            <Setter TargetName="FocusVisual" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <!--  Invalid  -->
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter TargetName="FocusVisual" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="HasLabel" Value="True">
                            <Setter TargetName="PART_ContentHost" Property="Grid.Row" Value="1" />
                            <Setter TargetName="PART_ContentHost" Property="Grid.RowSpan" Value="1" />
                            <Setter TargetName="WatermarkVisualElement" Property="Grid.Row" Value="1" />
                            <Setter TargetName="WatermarkVisualElement" Property="Grid.RowSpan" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource RadPaneStyle}" TargetType="telerik:RadPane">
        <Setter Property="mat:MaterialAssist.IsShadowDisabled" Value="True" />
        <Setter Property="Background" Value="Transparent" />
    </Style>
    <Style BasedOn="{StaticResource RadPaneGroupStyle}" TargetType="telerik:RadPaneGroup">
        <Setter Property="mat:MaterialAssist.IsShadowDisabled" Value="True" />
        <Setter Property="Background" Value="Transparent" />
    </Style>
    <Style BasedOn="{StaticResource AutoHideAreaStyle}" TargetType="telerik:AutoHideArea">
        <Setter Property="mat:MaterialAssist.IsShadowDisabled" Value="True" />
        <Setter Property="Background" Value="Transparent" />
    </Style>


    <!--  GRID CHECKBOX COLUMN DISABLED OPACITY  -->
    <ControlTemplate x:Key="GridViewCheckBoxTemplate" TargetType="grid:GridViewCheckBox">
        <Grid Width="13"
              Height="13"
              HorizontalAlignment="Left"
              VerticalAlignment="Center">
            <Grid Background="Transparent">
                <Border x:Name="PART_RootElement"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                <Border x:Name="IndeterminatePath"
                        Width="7"
                        Height="7"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="{TemplateBinding Foreground}"
                        Opacity="1"
                        Visibility="Collapsed" />
                <Path x:Name="CheckedPath"
                      Width="9"
                      Height="8"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M0,3 L3,6 L7,0"
                      FlowDirection="LeftToRight"
                      Opacity="1"
                      Stretch="Fill"
                      Stroke="{TemplateBinding Foreground}"
                      StrokeThickness="1.5"
                      Visibility="Collapsed" />
            </Grid>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CheckStates">
                    <VisualState x:Name="Checked">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames BeginTime="0"
                                                           Storyboard.TargetName="CheckedPath"
                                                           Storyboard.TargetProperty="(UIElement.Visibility)"
                                                           Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Unchecked" />
                    <VisualState x:Name="Indeterminate">
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames BeginTime="0"
                                                           Storyboard.TargetName="IndeterminatePath"
                                                           Storyboard.TargetProperty="(UIElement.Visibility)"
                                                           Duration="0">
                                <DiscreteObjectKeyFrame KeyTime="0">
                                    <DiscreteObjectKeyFrame.Value>
                                        <Visibility>Visible</Visibility>
                                    </DiscreteObjectKeyFrame.Value>
                                </DiscreteObjectKeyFrame>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
    </ControlTemplate>
    <Style x:Key="GridViewCheckBoxStyle" TargetType="grid:GridViewCheckBox">
        <Setter Property="Template" Value="{StaticResource GridViewCheckBoxTemplate}" />
        <Setter Property="Foreground" Value="{telerik:Office2019Resource ResourceKey=IconBrush}" />
        <Setter Property="Background" Value="{telerik:Office2019Resource ResourceKey=MainBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{telerik:Office2019Resource ResourceKey=MainBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style BasedOn="{StaticResource GridViewCheckBoxStyle}" TargetType="grid:GridViewCheckBox" />

    <Style x:Key="TreeViewItemNavigationTemplate" TargetType="telerik:RadTreeViewItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik:RadTreeViewItem">
                    <Grid x:Name="RootElement" UseLayoutRounding="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid x:Name="HeaderRow"
                              MinHeight="{TemplateBinding MinHeight}"
                              Background="Transparent"
                              SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="2"
                                    Grid.ColumnSpan="6"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="2" />
                            <Border x:Name="MouseOverVisual"
                                    Grid.Column="2"
                                    Grid.ColumnSpan="6"
                                    Background="{telerik:VisualStudio2013Resource ResourceKey=MouseOverBrush}"
                                    Opacity="0" />
                            <Border x:Name="SelectionUnfocusedVisual"
                                    Grid.Column="2"
                                    Grid.ColumnSpan="6"
                                    Background="{telerik:VisualStudio2013Resource ResourceKey=BasicBrush}"
                                    Visibility="Collapsed" />
                            <Border x:Name="SelectionVisual"
                                    Grid.Column="2"
                                    Grid.ColumnSpan="6"
                                    Background="{telerik:VisualStudio2013Resource ResourceKey=AccentMainBrush}"
                                    Visibility="Collapsed" />
                            <StackPanel x:Name="IndentContainer" Orientation="Horizontal">
                                <Rectangle x:Name="IndentFirstVerticalLine"
                                           Width="1"
                                           VerticalAlignment="Top"
                                           Stroke="{telerik:VisualStudio2013Resource ResourceKey=BasicBrush}"
                                           Visibility="Collapsed" />
                            </StackPanel>
                            <Grid x:Name="ListRootContainer"
                                  Grid.Column="1"
                                  MinWidth="21"
                                  HorizontalAlignment="Center">
                                <Rectangle x:Name="HorizontalLine"
                                           Height="1"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Stroke="{telerik:VisualStudio2013Resource ResourceKey=BasicBrush}" />
                                <Rectangle x:Name="VerticalLine"
                                           Width="1"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Top"
                                           Stroke="{telerik:VisualStudio2013Resource ResourceKey=BasicBrush}" />
                                <ToggleButton x:Name="Expander" Background="{TemplateBinding Background}" IsTabStop="False" />
                                <Grid x:Name="LoadingVisual"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RenderTransformOrigin="0.5,0.5"
                                      Visibility="Collapsed">
                                    <Grid.RenderTransform>
                                        <TransformGroup>
                                            <RotateTransform x:Name="LoadingVisualAngleTransform" Angle="0" CenterX="0.5" CenterY="0.5" />
                                        </TransformGroup>
                                    </Grid.RenderTransform>
                                    <Path Width="10"
                                          Height="10"
                                          Data="M1,0 A1,1,90,1,1,0,-1"
                                          Stretch="Fill"
                                          Stroke="{TemplateBinding Foreground}"
                                          StrokeStartLineCap="Round"
                                          StrokeThickness="1" />
                                    <Path Width="4"
                                          Height="4"
                                          Margin="5,-1.5,0,0"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Top"
                                          Data="M0,-1.1 L0.1,-1 L0,-0.9"
                                          Fill="{TemplateBinding Foreground}"
                                          Stretch="Fill"
                                          StrokeThickness="1" />
                                </Grid>
                            </Grid>
                            <Image x:Name="Image"
                                   Grid.Column="3"
                                   MaxWidth="16"
                                   MaxHeight="16"
                                   Margin="2"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                            <Rectangle x:Name="FocusVisual"
                                       Grid.Column="2"
                                       Grid.ColumnSpan="6"
                                       IsHitTestVisible="False"
                                       Stroke="{telerik:VisualStudio2013Resource ResourceKey=AccentMainBrush}"
                                       StrokeThickness="1"
                                       Visibility="Collapsed" />

                            <Grid x:Name="HeaderContentPanel"
                                  Grid.Column="4"
                                  Grid.ColumnSpan="2"
                                  Background="Transparent">
                                <ContentControl x:Name="Header"
                                                Margin="{TemplateBinding Padding}"
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                Content="{TemplateBinding Header}"
                                                ContentStringFormat="{TemplateBinding HeaderStringFormat}"
                                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                                Foreground="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Foreground, Mode=OneWay}"
                                                IsTabStop="False" />
                                <ContentPresenter x:Name="EditHeaderElement"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  ContentTemplate="{TemplateBinding HeaderEditTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding HeaderEditTemplateSelector}"
                                                  Visibility="Collapsed" />
                            </Grid>

                            <CheckBox x:Name="CheckBoxElement"
                                      Grid.Column="2"
                                      Margin="5,0,0,0"
                                      VerticalAlignment="Center"
                                      IsTabStop="False"
                                      Visibility="Collapsed" />
                            <RadioButton x:Name="RadioButtonElement"
                                         Grid.Column="2"
                                         Margin="5,0,0,0"
                                         VerticalAlignment="Center"
                                         IsTabStop="False"
                                         Visibility="Collapsed" />
                        </Grid>
                        <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Visibility="Collapsed" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Header"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.2"
                                                         Duration="0" />
                                        <DoubleAnimation Storyboard.TargetName="Image"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.2"
                                                         Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="ExpandStates">
                                <VisualState x:Name="Expanded">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ItemsHost" Storyboard.TargetProperty="Visibility" Duration="0">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Visible</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Collapsed" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>